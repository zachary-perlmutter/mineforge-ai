apiVersion: v1
kind: Service
metadata:
  name: mineforge-api
  namespace: minecraft
spec:
  type: NodePort
  selector:
    app: mineforge-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30090
      protocol: TCP
