apiVersion: v1
kind: Service
metadata:
  name: mineforge-web
  namespace: minecraft
spec:
  type: NodePort
  selector:
    app: mineforge-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
