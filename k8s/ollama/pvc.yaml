apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models
  namespace: ollama
spec:
  accessModes:
    - ReadWriteOnce
  # local-path avoids Longhorn replica scheduling constraints — models are re-downloadable
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
